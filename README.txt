la carpeta processOK contiene los procesos anteriores en donde, si el comando
no existe, borra el contenido anterior del outfile y no printa nada al llamar
al "cat out"

En el repo de Miranda, todo funciona bien. Checkear la parte en la que, cuando
llama al bin/ls, aunque diga que el cmd not found, debe funcionar igualmente.
Si ese path ya existe, directamente hazme un excev;
